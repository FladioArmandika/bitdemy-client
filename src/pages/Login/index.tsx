import React, { ReactElement } from 'react'

interface Props {
    
}

export default function LoginPage(props: Props): ReactElement {
    return (
        <div>
            Login
        </div>
    )
}
